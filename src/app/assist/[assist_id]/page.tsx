import React from "react";

export default async function assitDetails(props) {
  const params = await props.params;
  const searchParams = await props.searchParams;
  console.log(props);
  return <div>assitDetails</div>;
}
